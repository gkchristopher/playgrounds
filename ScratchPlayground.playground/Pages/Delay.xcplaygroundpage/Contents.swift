import Foundation

let delay = Double(Int64(10 * Double(USEC_PER_SEC))) / Double(NSEC_PER_SEC)

let milla = Double(USEC_PER_SEC)
let nano = Double(NSEC_PER_SEC)
let first = Int64(10 * Double(USEC_PER_SEC))
